# training

Optional model-training package.

Candidate models:

1. Action probability model.
2. Hidden-set classification model.
3. Value model for position evaluation.
4. Team compatibility model.

Training jobs consume versioned Parquet datasets. Models are promoted only through the evaluation pipeline.
